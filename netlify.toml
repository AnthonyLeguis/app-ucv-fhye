[build]
  publish = "client/dist" # Directorio de publicación (ajusta si es diferente)
  command = "npm run build" # Comando de construcción (ajusta si es diferente)

[functions]
  directory = "client/netlify/functions" # Directorio de funciones (ajusta si es diferente)